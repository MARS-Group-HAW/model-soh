SOHChristmasMarketBox

DISCLAIMER: README ist noch nicht fertig ausgefüllt. Wird auf dem aktuellen Stand gehalten.

--

Aktuelles Vorgehen: 

- dotnet build in \model-soh
- dotnet run in \model-soh\SOHChristmasMarketBox

Dabei beachten:

- Auswahl von Modell innerhalb von config.json und Program.cs ändern. Momentan ist nur RandomWalkMarketTraveler für Testzwecke nutzbar.

Klassen sind zu finden unter \model-soh\SOHModel\SOHModel\ChristmasMarketBox