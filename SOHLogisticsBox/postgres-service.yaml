# postgres-service.yaml
apiVersion: v1
kind: Service
metadata:
  name: postgres-service
  namespace: Username-default
spec:
  selector:
    app: postgres
  ports:
    - port: 5432
      targetPort: 5432
