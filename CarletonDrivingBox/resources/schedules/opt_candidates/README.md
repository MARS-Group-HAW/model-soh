# Candidate schedules from scripts/optimize_exit_assignment.py

Do not use these paths in `config_scenario_01.json` … `config_scenario_12.json`.
Generate / refresh with:

```bash
python scripts/optimize_exit_assignment.py --optimize --write-top 10
```
